# Agent instructions

Read [`agents.txt`](agents.txt) before changing this repository. That file is the source of truth for coding agents (architecture, invariants, file map, no-gos).

A copy is served by the web app at `/agents.txt`.

Do not edit [`apps/web/AGENTS.md`](apps/web/AGENTS.md). Next.js regenerates that block.
